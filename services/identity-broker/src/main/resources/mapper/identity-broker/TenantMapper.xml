<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.inventory.identity.adapter.out.persistence.TenantMapper">

    <resultMap id="tenantRow"
               type="com.example.inventory.identity.adapter.out.persistence.TenantRow">
        <constructor>
            <idArg column="tenant_id" javaType="java.lang.String"/>
            <arg column="display_name" javaType="java.lang.String"/>
            <arg column="status" javaType="java.lang.String"/>
            <arg column="created_at" javaType="java.time.Instant"/>
            <arg column="deactivated_at" javaType="java.time.Instant"/>
        </constructor>
    </resultMap>

    <insert id="insert">
        INSERT INTO tenants (tenant_id, display_name, status, created_at, deactivated_at)
        VALUES (#{tenantId}, #{displayName}, #{status}, #{createdAt}, #{deactivatedAt})
    </insert>

    <update id="updateStatus">
        UPDATE tenants
           SET display_name = #{displayName},
               status = #{status},
               deactivated_at = #{deactivatedAt}
         WHERE tenant_id = #{tenantId}
    </update>

    <select id="findById" resultMap="tenantRow">
        SELECT tenant_id, display_name, status, created_at, deactivated_at
          FROM tenants
         WHERE tenant_id = #{tenantId}
    </select>

    <select id="findAll" resultMap="tenantRow">
        SELECT tenant_id, display_name, status, created_at, deactivated_at
          FROM tenants
         ORDER BY tenant_id
    </select>

</mapper>
