<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.inventory.analytics.adapter.out.persistence.ProcessedEventMapper">

    <select id="existsByEventId" resultType="int">
        SELECT count(*) FROM processed_event WHERE event_id = #{eventId}
    </select>

    <insert id="insert">
        INSERT INTO processed_event (event_id, tenant_id, topic)
        VALUES (#{eventId}, #{tenantId}, #{topic})
    </insert>

</mapper>
